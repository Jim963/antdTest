import { Tabs, Table, Tag, Space, Button, DatePicker } from "antd";

const TablePage = () => {
  return (
    <div className="flex flex-row items-center justify-center">看一下</div>
  );
};

export default TablePage;
